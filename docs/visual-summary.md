# Visual Summary: LLM-Driven Architecture

## The Big Picture

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                         USER (Natural Language)                       │
│                  "Deploy MyApp to tenant ABC in dev"                 │
│                                                                       │
└───────────────────────────────┬───────────────────────────────────────┘
                                │
                                │ HTTP POST
                                ▼
                    ┌─────────────────────┐
                    │   API Gateway       │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │  Chat Handler       │
                    │  (Lambda)           │
                    └──────────┬──────────┘
                               │
                               │ invoke_agent()
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                    ORCHESTRATOR AGENT (LLM)                          │
│                    Foundation Model: AWS Nova Pro                    │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  LLM BRAIN:                                                 │    │
│  │  • Reads natural language instruction                       │    │
│  │  • Searches knowledge base (RAG) ──────────────────┐       │    │
│  │  • Queries memory (DynamoDB) ──────────────────┐   │       │    │
│  │  • Decides which agents to invoke              │   │       │    │
│  │  • Generates natural language responses        │   │       │    │
│  └────────────────────────────────────────────────┼───┼───────┘    │
│                                                    │   │             │
│  Available Tools (Action Groups):                 │   │             │
│  • invoke-specialized-agent ──────────────────┐   │   │             │
│  • memory-operations ─────────────────────┐   │   │   │             │
│                                           │   │   │   │             │
└───────────────────────────────────────────┼───┼───┼───┼─────────────┘
                                            │   │   │   │
                    ┌───────────────────────┘   │   │   │
                    │                           │   │   │
                    ▼                           ▼   │   │
        ┌─────────────────────┐    ┌─────────────────────┐
        │  Memory Manager     │    │  Knowledge Base     │
        │  (Lambda)           │    │  (S3 Vector Store)  │
        │                     │    │                     │
        │  • Query history    │    │  • Orchestration    │
        │  • Store results    │    │    patterns         │
        │  • Check state      │    │  • Dependencies     │
        └─────────────────────┘    │  • Best practices   │
                                   └─────────────────────┘
                    │
                    │ Orchestrator decides to invoke Bootstrap Agent
                    ▼
        ┌─────────────────────┐
        │  Agent Invoker      │
        │  (Lambda)           │
        │                     │
        │  invoke_agent(      │
        │    agent="bootstrap"│
        │    instruction="..." │
        │  )                  │
        └──────────┬──────────┘
                   │
                   │ Natural language instruction
                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                    BOOTSTRAP AGENT (LLM)                             │
│                    Foundation Model: AWS Nova Pro                    │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  LLM BRAIN:                                                 │    │
│  │  • Understands instruction from orchestrator                │    │
│  │  • Searches knowledge base (RAG) ──────────────────┐       │    │
│  │  • Decides which MCP tools to call                 │       │    │
│  │  • Monitors execution                              │       │    │
│  │  • Generates response                              │       │    │
│  └────────────────────────────────────────────────────┼───────┘    │
│                                                        │             │
│  Available Tools (Action Groups):                     │             │
│  • execute-bootstrap-pipeline ────────────────┐       │             │
│  • get-pipeline-status                        │       │             │
│                                               │       │             │
└───────────────────────────────────────────────┼───────┼─────────────┘
                                                │       │
                    ┌───────────────────────────┘       │
                    │                                   │
                    ▼                                   ▼
        ┌─────────────────────┐          ┌─────────────────────┐
        │  MCP Proxy          │          │  Knowledge Base     │
        │  (Lambda)           │          │  (S3 Vector Store)  │
        │                     │          │                     │
        │  • Routes to MCP    │          │  • VPC design       │
        │  • Stores in memory │          │  • Networking       │
        │  • Returns results  │          │  • Best practices   │
        └──────────┬──────────┘          └─────────────────────┘
                   │
                   │ HTTP POST
                   ▼
        ┌─────────────────────┐
        │  MCP Server         │
        │  (HTTP Server)      │
        │                     │
        │  • Execute pipeline │
        │  • Create VPC       │
        │  • Create subnets   │
        │  • Configure ACLs   │
        └──────────┬──────────┘
                   │
                   │ Results
                   ▼
        ┌─────────────────────┐
        │  DynamoDB           │
        │  (Memory)           │
        │                     │
        │  Store execution:   │
        │  • environment: dev │
        │  • pipeline: boot   │
        │  • status: complete │
        └─────────────────────┘
                   │
                   │ Results flow back
                   ▼
            Bootstrap Agent LLM
                   │
                   │ Natural language response
                   ▼
            Orchestrator Agent LLM
                   │
                   │ Decides to invoke Compute Agent next
                   ▼
            [Similar flow for Compute Agent]
                   │
                   ▼
            [Similar flow for App Agent]
                   │
                   │ Final response
                   ▼
                  USER
```

## Key Insight: Where Decisions Happen

```
┌─────────────────────────────────────────────────────────────┐
│                                                               │
│  ❌ NOT HERE (No hardcoded logic):                           │
│                                                               │
│  if request.type == "deploy":                                │
│      if not bootstrap_exists():                              │
│          run_bootstrap()                                     │
│      run_compute()                                           │
│      run_app()                                               │
│                                                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                                                               │
│  ✅ HERE (LLM reasoning):                                     │
│                                                               │
│  Orchestrator LLM receives: "Deploy to tenant ABC"           │
│                                                               │
│  LLM thinks:                                                  │
│  1. Let me search my knowledge base...                       │
│     → Found: "Check memory before deploying"                 │
│                                                               │
│  2. Let me query memory...                                   │
│     → Bootstrap exists ✓                                     │
│     → Compute exists for ABC ✓                               │
│                                                               │
│  3. According to my knowledge, I only need App Agent         │
│                                                               │
│  4. Let me invoke App Agent with instruction:                │
│     "Deploy application to tenant ABC..."                    │
│                                                               │
│  [No if/else in code - pure LLM reasoning!]                  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## Component Roles

```
┌──────────────────────────────────────────────────────────────────┐
│  Component                    │  Role                            │
├──────────────────────────────────────────────────────────────────┤
│  bedrock_agents.tf            │  🧠 Defines LLM brains          │
│  bedrock_knowledge_base.tf    │  📚 Sets up RAG                 │
│  bedrock_action_groups.tf     │  🔧 Defines available tools     │
│  knowledge_base/*.md          │  📖 Content for RAG             │
│  agent_invoker.py             │  🤝 LLM-to-LLM communication    │
│  mcp_proxy.py                 │  🔌 LLM-to-tool bridge          │
│  memory_manager.py            │  💾 State for LLM decisions     │
│  http_server.py               │  ⚙️  Actual pipeline execution  │
└──────────────────────────────────────────────────────────────────┘
```

## Data Flow Summary

```
User Message (Natural Language)
    ↓
Orchestrator LLM
    ├─→ RAG Search (automatic)
    ├─→ Memory Query (via action group)
    └─→ Agent Invocation (via action group)
        ↓
Specialized Agent LLM
    ├─→ RAG Search (automatic)
    └─→ MCP Tool Call (via action group)
        ↓
MCP Server (actual execution)
    ↓
Results stored in Memory
    ↓
Response flows back through LLMs
    ↓
User receives Natural Language Summary
```

## The Magic: RAG in Action

```
┌─────────────────────────────────────────────────────────────┐
│  User: "I need to deploy to a new environment"              │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  Orchestrator LLM automatically searches knowledge base:     │
│                                                               │
│  Query: "new environment deployment requirements"            │
│                                                               │
│  Retrieved from knowledge_base/orchestrator/:                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ## New Environment Setup                            │   │
│  │ When setting up a new environment:                  │   │
│  │ 1. Check if bootstrap has run                       │   │
│  │ 2. If not, delegate to Bootstrap Agent first        │   │
│  │ 3. Then delegate to Compute Agent                   │   │
│  │ 4. Finally delegate to App Agent                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  LLM uses this context to make decision!                     │
└─────────────────────────────────────────────────────────────┘
```

## Memory-Driven Decisions

```
┌─────────────────────────────────────────────────────────────┐
│  Scenario 1: Nothing exists                                  │
│                                                               │
│  Memory Query Result: []                                     │
│                                                               │
│  LLM Decision: "Need Bootstrap → Compute → App"              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Scenario 2: Bootstrap exists                                │
│                                                               │
│  Memory Query Result: [{pipeline: "bootstrap", ...}]         │
│                                                               │
│  LLM Decision: "Skip Bootstrap, need Compute → App"          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Scenario 3: Everything exists                               │
│                                                               │
│  Memory Query Result: [                                      │
│    {pipeline: "bootstrap", ...},                             │
│    {pipeline: "compute", tenant: "ABC", ...},                │
│    {pipeline: "app", tenant: "ABC", ...}                     │
│  ]                                                            │
│                                                               │
│  LLM Decision: "Everything exists, nothing to do"            │
└─────────────────────────────────────────────────────────────┘
```

## Why This Is Better Than Static Routing

```
┌─────────────────────────────────────────────────────────────┐
│  Static Routing Problems:                                    │
│                                                               │
│  • Hardcoded if/else for every scenario                      │
│  • Can't handle unexpected requests                          │
│  • Requires code changes for new patterns                    │
│  • No natural language understanding                         │
│  • Fixed workflows                                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  LLM-Driven Benefits:                                        │
│                                                               │
│  ✅ Understands natural language                             │
│  ✅ Adapts to context dynamically                            │
│  ✅ Uses knowledge base for guidance                         │
│  ✅ Queries memory for state                                 │
│  ✅ Makes intelligent decisions                              │
│  ✅ Generates natural language responses                     │
│  ✅ No code changes for new patterns                         │
│  ✅ Flexible workflows                                       │
└─────────────────────────────────────────────────────────────┘
```

## The Bottom Line

```
┌─────────────────────────────────────────────────────────────┐
│                                                               │
│  Traditional System:                                         │
│  Code tells the system what to do                            │
│                                                               │
│  This System:                                                │
│  LLM figures out what to do based on:                        │
│  • Instructions (guidance)                                   │
│  • Knowledge base (patterns)                                 │
│  • Memory (state)                                            │
│  • Available tools (capabilities)                            │
│                                                               │
│  The orchestration is emergent, not programmed!              │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```
